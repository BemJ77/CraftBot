return {
    background = colors.black,
    text = colors.white,
    title = colors.lime,
    subtitle = colors.cyan,
    selected = colors.yellow,
    muted = colors.lightGray,
    success = colors.lime,
    warning = colors.orange,
    error = colors.red,
    progressFilled = colors.lime,
    progressEmpty = colors.gray
}
