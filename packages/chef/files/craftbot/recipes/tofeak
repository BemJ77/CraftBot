local items = require("items")
 
return {
    id = "tofeak",
    label = "tofeak",
 
    visible = false,
    
    output = items.tofeak,
    outputCount = 2,
 
    ingredients = {
        [1] = items.mixing,
        [2] = items.firmtofu,
        [3] = items.poivre,
        [5] = items.saucesoja,
    },

	returns = {
        items.mixing
    }
}
