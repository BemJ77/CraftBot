local items = require("items")
 
return {
    id = "groundbeef",
    label = "groundbeef",
 
    visible = false,
    
    output = items.groundbeef,
    outputCount = 2,
 
    ingredients = {
        [1] = items.grinder,
        [2] = items.tofeak,
    },

	returns = {
        items.grinder
    }
}
