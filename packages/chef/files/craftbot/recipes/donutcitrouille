local items = require("items")
 
return {
    id = "donutcitrouille",
    label = "Donut à la Citrouille",
 
    visible = true,
    category = "dessert",

    output = items.donutcitrouille,
    outputCount = 1,
 
    ingredients = {
        [1] = items.pot,
        [2] = items.huile,
        [3] = items.citrouille,
        [5] = items.pate,
    },

	returns = {
        items.pot
    }
}
