local items = require("items")
 
return {
    id = "donutpomme",
    label = "Donut à la Pomme",
 
    visible = true,
    category = "dessert",

    output = items.donutpomme,
    outputCount = 1,
 
    ingredients = {
        [1] = items.pot,
        [2] = items.huile,
        [3] = items.pomme,
        [5] = items.pate,
    },

	returns = {
        items.pot
    }
}
